dependencies:
  override:
    - go get -t -d -v ./...
    - go build -v

test:
  pre:
    - go vet $(go list ./... | grep -v /vendor/)
  override:
    - go test -cpu=2 -race -v -covermode=atomic ./...
